apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    eventlistener: {{ include ".helm.fullname" . }}
    {{- include "quarkus-pipeline.labels" . | nindent 4 }}    
  name: {{ include ".helm.fullname" . }}
spec:
  host: {{ include ".helm.fullname" . }}-{{ .Release.Namespace}}/{{ .Values.baseURL }}
  port:
    targetPort: http-listener
  to:
    kind: Service
    name: {{ include ".helm.fullname" . }}
    weight: 100
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None